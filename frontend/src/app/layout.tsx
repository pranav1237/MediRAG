import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-geist-sans",
  display: "swap",
});

export const metadata: Metadata = {
  title: "MediRAG – Clinical Knowledge Assistant",
  description:
    "AI-powered clinical knowledge retrieval system providing evidence-grounded answers with source citations from approved medical documents.",
  keywords: ["MediRAG", "clinical AI", "RAG", "medical guidelines", "healthcare assistant"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${inter.variable} h-full antialiased`}>
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
